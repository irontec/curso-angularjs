- Create
    ```javascript
    Service.$save(function(service) {
       $location.url('/');
    });
    ```

- Update
    ```javascript
    Service.$update(function(service) {
       $location.url('/');
    });
    ```
